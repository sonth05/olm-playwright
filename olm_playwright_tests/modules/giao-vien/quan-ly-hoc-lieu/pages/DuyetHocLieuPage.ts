import { BasePage } from '@core/shared-pages/BasePage';

/**
 * Page Object — Duyệt học liệu (2.4.3).
 * TODO: Chưa triển khai — bổ sung selectors & method khi có yêu cầu cụ thể.
 * Các học liệu được tạo mới cần đưa qua trường để duyệt: list các danh sách, bộ lọc, trạng thái.
 */
export class DuyetHocLieuPage extends BasePage {
  // TODO: implement
}
