name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: playwright-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    name: Smoke tests (PR)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        working-directory: olm_playwright_tests
    env:
      CI: true
      HEADLESS: true
      OLM_VIP_USERNAME: ${{ secrets.OLM_VIP_USERNAME }}
      OLM_VIP_PASSWORD: ${{ secrets.OLM_VIP_PASSWORD }}
      OLM_SCHOOL_USERNAME: ${{ secrets.OLM_SCHOOL_USERNAME }}
      OLM_SCHOOL_PASSWORD: ${{ secrets.OLM_SCHOOL_PASSWORD }}
      OLM_NORMAL_USERNAME: ${{ secrets.OLM_NORMAL_USERNAME }}
      OLM_NORMAL_PASSWORD: ${{ secrets.OLM_NORMAL_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: olm_playwright_tests/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps

      - name: Run smoke tests
        run: npm run test:smoke

      - name: Upload report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-smoke-report
          path: |
            olm_playwright_tests/reports/
          retention-days: 14

  full:
    name: Full test suite (main)
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 180
    defaults:
      run:
        working-directory: olm_playwright_tests
    env:
      CI: true
      HEADLESS: true
      OLM_VIP_USERNAME: ${{ secrets.OLM_VIP_USERNAME }}
      OLM_VIP_PASSWORD: ${{ secrets.OLM_VIP_PASSWORD }}
      OLM_SCHOOL_USERNAME: ${{ secrets.OLM_SCHOOL_USERNAME }}
      OLM_SCHOOL_PASSWORD: ${{ secrets.OLM_SCHOOL_PASSWORD }}
      OLM_NORMAL_USERNAME: ${{ secrets.OLM_NORMAL_USERNAME }}
      OLM_NORMAL_PASSWORD: ${{ secrets.OLM_NORMAL_PASSWORD }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: olm_playwright_tests/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps

      - name: Run full test suite
        run: npm test

      - name: Upload report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-full-report
          path: |
            olm_playwright_tests/reports/
          retention-days: 30
